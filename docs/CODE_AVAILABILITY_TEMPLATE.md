# Code availability and software citation template

Do not replace the bracketed fields until the public record resolves and the licence has institutional approval.

## Ready-to-paste after DOI release

```text
Code availability
The source code supporting the BR-SAT study, including five-image input preprocessing, frozen ensemble inference, examination-level probability aggregation, the development-selected q_R threshold rule for reactive versus non-reactive interpretation, statistical evaluation, and generation of the quantitative figures and tables, is publicly available in the BR-SAT GitHub repository at [GITHUB URL] and is archived in Zenodo at [DOI], release v1.0.0, under the BSD-3-Clause License. The repository includes dependency specifications, configuration files, documentation, and synthetic example inputs. The frozen model weights used to generate the reported results are not included in the public release because [VERIFIED RESTRICTION RATIONALE]. [VERIFIED ACCESS CONDITIONS OR REQUEST ROUTE]. No participant-level images, hospital-identifying metadata, or other confidential clinical data are included in the repository.
```

## Formal code reference

```text
Yuan, W. (2026). BR-SAT: protocol-constrained five-image inference and evaluation code (version 1.0.0). Zenodo. [DOI]
```

Use the Zenodo DOI in the reference list rather than citing only the moving GitHub default branch. Add the GitHub URL in the Code availability section for reader convenience, while treating the DOI archive as the fixed paper-linked version.
